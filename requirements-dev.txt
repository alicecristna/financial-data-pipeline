# Minimal dependencies for tests and the offline demo.
# The full pipeline additionally needs yfinance and baostock for live
# fetching (see requirements.txt); the offline test suite never imports them.
pandas>=2.2,<4
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pytest>=8.0
